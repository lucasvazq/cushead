language: python
python:
  - '3.8'
notifications:
  email: false
cache: pip
install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt
script:
  - coverage run test/tests.py
  - codecov
