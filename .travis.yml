language: python
python:
  - "3.8"
notifications:
  email: false
cache: pip
install:
  - pip install codecov
  - python3 -m pip install coverage
  - pip install -r requirements.txt
script:
  # success
  - python3 -m coverage run -a cushead.py -default test/test.json --images
  - python3 -m coverage run -a cushead.py -config test/test.json
  - python3 -m coverage run -a cushead.py -h
  # bad
  - python3 -m coverage run -a cushead.py
  - python3 -m coverage run -a cushead.py .
  - python3 -m coverage run -a cushead.py -default
  - python3 -m coverage run -a cushead.py -config
  - python3 -m coverage run -a cushead.py -config . --images
  - python3 -m coverage run -a cushead.py -config .
  - codecov