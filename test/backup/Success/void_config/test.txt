"""
Python syntax
cushead config file
Git: https://github.com/lucasvazq/cushead
Documentation: https://github.com/lucasvazq/cushead/blob/master/README.md

CONFIG VARIABLES:

    html_file (FILE PATH):
        Required, can't be void, need to exist and refer a file

    output (FOLDER PATH):
        Required, need to exist and refer a folder

    static_url (STRING):
        Required

    icon_png (FILE PATH):
        If declared, need to exist and refer a image file
        Recomended 1024x1024 png image
"""

# Don't delete or change the name of this variable
config = {

    # MAIN CONFIG
    'html_file':        './test/tests/Success/void_config/index.html',
    'output':           './test/tests/Success/void_config/', # e.g. for manifest.json
    'static_url':       '/static/'
}
